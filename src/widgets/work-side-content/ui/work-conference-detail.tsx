import { useSideModalStore } from '@/shared/store'
import { Button } from '@/shared/ui'

export const WorkConferenceDetail = () => {
  const closeSideModal = useSideModalStore(state => state.closeSideModal)
  return <div className={'flex min-h-0 w-full flex-1 flex-col gap-[10px] px-[20px] lg:w-[750px] lg:px-[30px]'}>sd</div>
}
